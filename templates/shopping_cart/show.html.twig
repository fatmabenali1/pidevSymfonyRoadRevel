{% extends 'base.html.twig' %}

{% block title %}ShoppingCart{% endblock %}

{% block body %}
    <h1>ShoppingCart</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCart</th>
                <td>{{ shopping_cart.idCart }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ shopping_cart.price }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ shopping_cart.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_shopping_cart_index') }}">back to list</a>

    <a href="{{ path('app_shopping_cart_edit', {'idCart': shopping_cart.idCart}) }}">edit</a>

    {{ include('shopping_cart/_delete_form.html.twig') }}
{% endblock %}
