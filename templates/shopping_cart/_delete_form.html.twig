<form method="post" action="{{ path('app_shopping_cart_delete', {'idCart': shopping_cart.idCart}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shopping_cart.idCart) }}">
    <button class="btn">Delete</button>
</form>
